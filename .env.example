# Supabase Configuration
# Get these from your Supabase project settings
VITE_SUPABASE_PROJECT_ID="your-project-id"
VITE_SUPABASE_PUBLISHABLE_KEY="your-anon-key"
VITE_SUPABASE_URL="https://your-project.supabase.co"

# Google Gemini API Key
# IMPORTANT: This should be set in Supabase Edge Functions environment, NOT here for production
# Set this as GEMINI_API_KEY in your Supabase Edge Functions secrets
GEMINI_API_KEY="your-gemini-api-key"

# Single Account Access
# Set the email address that is allowed to access the application
# Only this email can log in
# For production, use environment variable: VITE_ALLOWED_EMAIL
ALLOWED_EMAIL="your-email@example.com"
